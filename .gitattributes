# Normalize text files in repo as LF.
* text=auto eol=lf

# Keep Windows-native script files as CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf

# Common binaries should never be line-normalized.
*.png -text
*.jpg -text
*.jpeg -text
*.gif -text
*.pdf -text
*.zip -text
*.dll -text
*.exe -text
